<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Nodejs Passport Local</title>
    <link rel="stylesheet" href="https://bootswatch.com/lux" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
    />
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Login Node Paasport</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if (user) {%>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/profile"
                >Profile</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/logout"
                >Logout</a
              >
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/login"
                >Login</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/register"
                >Register</a
              >
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
    <div class="conteiner">
      <% if (loginMessage.length > 0) { %>
      <div class="alert alert-danger"><%= loginMessage %></div>
      <% } %> <%if (registerMessage.length > 0) {%>
      <div class="alert alert-danger"><%= registerMessage %></div>
      <% } %>
    </div>
  </body>
</html>
